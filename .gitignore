node_modules

# Build output — generated by `npm run build`.
# Vercel builds from source (vercel.json: buildCommand + outputDirectory),
# so this does not need to be committed.
dist/

# Environment files — never commit real keys. Use .env.example as the template.
.env
.env.local
.env.*.local
